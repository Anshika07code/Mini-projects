import random

while True:
    input("Press Enter to roll the dice...")
    print("You rolled:", random.randint(1, 6))

    choice = input("Roll again? (y/n): ")
    if choice.lower() != 'y':
        break
