using System;
using System.Threading;

class Clock
{
    static void Main()
    {
        while (true)
        {
            Console.Clear();
            Console.WriteLine(DateTime.Now.ToLongTimeString());
            Thread.Sleep(1000);
        }
    }
}
